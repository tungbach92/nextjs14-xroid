# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Create Release
'on':
  pull_request:
    branches:
      - main
      - releases/**
    types:
      - closed

env:
  GH_TOKEN: ${{ github.token }}

jobs:
  create-release:
    name: wf-reuse
    uses: Andom-Geniam/reusable-workflows/.github/workflows/create-release.yaml@main
